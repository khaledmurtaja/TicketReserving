class TicketTransaction {
  dynamic? movieName;
  dynamic? numOfTickets;
  TicketTransaction({required dynamic movieName, required dynamic numOfTickets}) {
    this.movieName = movieName;
    this.numOfTickets = numOfTickets;
  }
}
